import type { ServiceRequest } from "./serviceRequest";

export interface CyclistServiceRequestHistoryResponse {
    current_page: number;
    data: ServiceRequest[];
    first_page_url: string;
    from: number | null;
    last_page: number;
    last_page_url: string;
    links: {
        url: string | null;
        label: string;
        page: number | null;
        active: boolean;
    }[];
    next_page_url: string | null;
    path: string;
    per_page: number;
    prev_page_url: string | null;
    to: number | null;
    total: number;
}
